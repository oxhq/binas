# binas-core

Shared checked spans, selectors, diagnostics, and verification types for Binas format engines.

Most PDF applications should depend on [`binas-pdf`](https://crates.io/crates/binas-pdf) instead. This crate exists as the small format-neutral dependency used by Binas engine crates.

Licensed under Apache-2.0.
